{
  "name": "ai-chat-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "mocha --require test/setup.cjs --exit test/api.test.js",
    "test:all": "mocha --require test/setup.cjs --exit test/**/*.test.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "better-sqlite3": "^11.7.0",
    "env-paths": "^4.0.0",
    "fastify": "^5.0.0",
    "node-llama-cpp": "^3.17.1"
  },
  "devDependencies": {
    "mocha": "^11.7.5",
    "supertest": "^7.2.2"
  }
}
