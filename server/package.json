{
  "name": "ai-chat-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "mocha --exit test/**/*.test.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "better-sqlite3": "^11.7.0",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "mocha": "^11.7.5",
    "supertest": "^7.2.2"
  }
}
